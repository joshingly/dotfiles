#!/usr/bin/env ruby

require "exifr"
require "fileutils"

pictures = Dir["*.jpg", "*.JPG"]

pictures.each do |picture|
  puts picture
  new_name = EXIFR::JPEG.new(picture).date_time.strftime("%m-%d-%y--%I.%M.%S%P.jpg")
  begin
    FileUtils.mv(picture, new_name)
  rescue; end;
  puts "--"
end
