#!/usr/bin/env ruby

now = Time.now

day = sprintf '%02d', now.day
month = sprintf '%02d', now.month
year = now.year

name = "#{year}-#{month}-#{day}.md"
path = File.expand_path "~/Dropbox/Wiki/Journal/#{name}"

unless File.exists?(path)
  IO.write(path, "# #{year}-#{month}-#{day}\n")
end
