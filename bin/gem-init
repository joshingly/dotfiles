#!/usr/bin/env ruby

location = `which ruby`

fail "don't use the system ruby" if location =~ /\/usr\/bin\/ruby/

system 'gem install awesome_print pry-byebug forward middleman rails cocoapods fastlane'
