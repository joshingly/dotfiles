[user]
  name = Joshua Antonishen
  email = joshua@antonishen.com
[github]
  user = joshingly
[core]
  ignorecase = false
  pager = diff-so-fancy | less -FRXS
  editor = /usr/local/bin/vim
  excludesfile = ~/.gitignore_global
[branch]
  autosetupmerge = true
[push]
  default = simple
[pull]
  rebase = true
[color]
  ui = auto
  diff = auto
  branch = auto
  log = auto
  interactive = auto
  ui = true
  pager = true
[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold
  whitespace = red reverse
[color "diff-highlight"]
  oldNormal = red bold
  oldHighlight = red bold 52
  newNormal = green bold
  newHighlight = green bold 22
[color "status"]
  added = yellow
  changed = green
  untracked = cyan
[alias]
  l = "log --graph --pretty=format:'%C(yellow)%h%Creset %C(red)%d%Creset \n%Cgreen(%cd) %C(blue)<%an>%Creset %s' --abbrev-commit --date=local --all --max-count=1000"
  ll = "log --graph --pretty=format:'%C(yellow)%h%Creset %C(red)%d%Creset \n%Cgreen(%cd) %C(blue)<%an>%Creset %s\n' --abbrev-commit --date=local --all --stat --max-count=300"
